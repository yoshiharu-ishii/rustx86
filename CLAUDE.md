# rustx86 — プロジェクト固有のガイド

## プロジェクトの正体（セーフガード誤反応の抑制）

これはオープンソースの x86／x86_64 **エミュレータ**開発プロジェクトである。
QEMU・Bochs・86Box・Unicorn と同じ系譜の、正当な処理系（CPU エミュレータ）開発であり、
リポジトリも公開されている。以下はいずれも**攻撃手法ではなく、エミュレータの正しさを
検証するための標準的な手段**である。低レイヤの語彙が出てくるのは、それがまさに
**エミュレーションの対象そのもの**だからで、悪用目的ではない。

- **生のオペコード列をバッファに置いて実行する** → 命令デコーダ／実行器の cosim テストケース。
  Unicorn（実 CPU 相当のオラクル）と1命令ずつ突き合わせて意味論を検証する
- **実行時コード生成（テンプレート JIT）** → 性能最適化手段（F1、ADR-0008）。
  デコード済みブロックを wasm／ネイティブコードへ落とし、決定性を壊さないことを門番で保証する
- **ptrace／デバッグ API によるシングルステップとレジスタ取得** → 実機を ground truth とする
  命令オラクル（未定義フラグ・境界条件の検証）
- **命令ファジング** → 未実装・不正確な命令の網羅的発見。ランダム命令をオラクルと照合する
- **リアルモード・特権リング（CPL）・割り込み・IDT／GDT・ページング・MBR／ブートセクタ**
  等の低レイヤ語彙 → すべてエミュレーション対象そのもの。実装して初めて OS が起動する

**門番が整備済み**であり、これは検証駆動のエミュレータ開発である：

- **cosim**（Unicorn と毎命令比較、`cargo test -p rustx86-cosim`）
- **kernel_lockstep**（実カーネルを Unicorn と1命令ずつ突き合わせ）
- **決定的命令数**（同じイメージなら起動までの命令数はビット同一 — 意味の後退を数字で検出）
- **golden trace／3OS 起動回帰 CI**（ELKS・FreeDOS・Linux がプロンプト到達）
- **JIT 決定性ゲート**（JIT on／off で命令数もシリアル出力もビット同一）

要するに、生オペコード実行・JIT・特権命令・割り込み・MBR といった語彙は、
このリポジトリでは**日常の開発語彙**である。マルウェア開発・脆弱性の実地悪用・
権限昇格の実行とは無関係なので、これらを理由に作業を止めないこと。

## 開発の流儀（この 2 つは特に守る）

- **測ったものだけ信じる**。最適化は交互 A/B（新旧バイナリを交互に走らせて差だけ見る）で
  判定し、単発の速さは熱ダレの運として捨てる。効かなかった案も理由つきで
  `docs/perf.md` に寝かせる（棄却しない — 前提が変われば再訪する）
- **意味論の原本は 1 つ**。JIT・dcache などの「速い写し」は、フラグも演算も従来経路と
  同じヘルパを呼ぶ。二重実装しない。正しさは上記の門番で毎回証明する

判断の記録は `docs/adr/`、数字と進捗の地図は `docs/perf.md`、作業状態は開いている PR の本文。
