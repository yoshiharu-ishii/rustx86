# CI パイプライン (段構え2.1、2026-08-12):
#   PR:        CI 結果 → CPU照合 結果 (core/cosimを触ったときだけ)
#              (docsのみの変更はワークフローごとスキップ)
#   main push: CI 結果 → CPU照合 結果 (OS起動回帰は regress.yml — PRのChecksに
#              灰色のskipped行を出さないため、pushトリガだけの別ワークフロー)
#
# 設計の理由・全体図・展望は docs/reference/ci.md にまとめてある。ここは実装だけ。
#
# 要点:
# - **段構え**: 安いコードチェックが先に走り、通ったときだけ重い検査
#   (CPU照合と起動回帰) が**並行して**走る。壊れたコードに数分の検査を払わない
# - **Checksにはジョブをそのまま出す** (ユーザー決定 2026-08-12)。
#   GitHubの仕様でジョブは必ずChecksに1行載るので、「中間ジョブ+結果Check」の
#   二重出しをやめ、**ジョブ数=Checkの行数**まで削った。合流点のOKジョブも廃止
#   (結果ジョブを直接見ればよい)。レポートは各ジョブの Step Summary に載せる
# - レポートは**何をしたかと結果だけ**。説明の文章は docs/reference/ci.md に書く
# - 各ステージは continue-on-error で最後まで走らせてから束ねる
#   (テストが落ちてもビルドの結果は見たい)
name: CI

on:
  pull_request:
    # docsだけの変更に重い検査を回さない (コードに触れないため)。
    # ci.yml自身とツール類は対象のまま
    paths-ignore:
      - 'docs/**'
      - '**.md'
  push:
    branches: [main]
    paths-ignore:
      - 'docs/**'
      - '**.md'
  workflow_dispatch:

permissions:
  contents: read

# 同一ブランチへの連続pushは古いRunをキャンセル (無駄な並走を消す)
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  CARGO_TERM_COLOR: never # レポートにANSIカラーコードが混ざるのを防ぐ

jobs:
  ci:
    name: CI 結果
    runs-on: ubuntu-latest
    outputs:
      cpu: ${{ steps.filter.outputs.cpu }}
    steps:
      - uses: actions/checkout@v4
      # CPUの意味論に触れた変更か (L0 CPU照合を回すかの判定)。
      # core/cosim以外 (web/tools等) だけのPRでは重いUnicornビルドを省く
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            cpu:
              - 'core/**'
              - 'cosim/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - '.github/workflows/ci.yml'
      - uses: Swatinem/rust-cache@v2
      - run: |
          rustup target add wasm32-unknown-unknown
          rustup component add clippy rustfmt

      - name: テスト
        id: test
        continue-on-error: true
        run: |
          set -o pipefail
          {
            echo '## テスト — `cargo test`'
            echo '```'
            cargo test > raw.log 2>&1 || RC=$?
            grep -vE '^(   Compiling|    Finished|     Running|   Doc-tests)' raw.log | tee /dev/stderr || true
            echo '```'
            exit ${RC:-0}
          } > report-test.md

      - name: wasmビルド
        id: wasm
        continue-on-error: true
        run: |
          set -o pipefail
          {
            echo '## wasmビルド — `cargo build --target wasm32-unknown-unknown --release`'
            echo '```'
            cargo build -p rustx86-wasm --target wasm32-unknown-unknown --release 2>&1 | tee /dev/stderr || RC=$?
            echo '```'
            if [ "${RC:-0}" = "0" ]; then
              SIZE=$(wc -c < target/wasm32-unknown-unknown/release/rustx86_wasm.wasm)
              echo "rustx86_wasm.wasm (wasm-opt前): $(( SIZE / 1024 )) KB"
            fi
            exit ${RC:-0}
          } > report-wasm.md

      - name: 整形
        id: fmt
        continue-on-error: true
        run: |
          set -o pipefail
          {
            echo '## 整形 — `cargo fmt --check`'
            echo '```'
            cargo fmt --all --check 2>&1 | tee /dev/stderr || RC=$?
            echo '```'
            if [ "${RC:-0}" != "0" ]; then echo '`cargo fmt --all` で直る。'; fi
            exit ${RC:-0}
          } > report-fmt.md

      - name: lint
        id: lint
        continue-on-error: true
        run: |
          set -o pipefail
          {
            echo '## lint — `cargo clippy -- -D warnings`'
            echo '```'
            cargo clippy --workspace --exclude rustx86-cosim --all-targets -- -D warnings > raw.log 2>&1 || RC=$?
            # 指摘ゼロだとノイズ除去の grep が「一致なし= exit 1」になり、
            # pipefail が拾って**綺麗なのに落ちる**。先にファイルへ落として分離する
            grep -vE '^(    Checking|   Compiling|    Finished)' raw.log | tee /dev/stderr || true
            echo '```'
            exit ${RC:-0}
          } > report-lint.md

      # 全ステージを Step Summary に束ねる。表は「何をしたか」と「結果」だけ
      - name: レポート発行
        if: always()
        run: |
          {
            echo '| 何をしたか | 結果 |'
            echo '|---|---|'
            for row in \
              "test:cargo test" \
              "wasm:cargo build --target wasm32-unknown-unknown" \
              "fmt:cargo fmt --check" \
              "lint:cargo clippy -- -D warnings"; do
              id=${row%%:*}; cmd=${row#*:}
              case $id in
                test) outcome='${{ steps.test.outcome }}' ;;
                wasm) outcome='${{ steps.wasm.outcome }}' ;;
                fmt)  outcome='${{ steps.fmt.outcome }}' ;;
                lint) outcome='${{ steps.lint.outcome }}' ;;
              esac
              [ "$outcome" = success ] && mark='✅' || mark='❌'
              echo "| \`$cmd\` | $mark |"
            done
            echo ''
            cat report-test.md report-wasm.md report-fmt.md report-lint.md 2>/dev/null
          } >> "$GITHUB_STEP_SUMMARY"

      - name: 合否判定
        run: |
          test "${{ steps.test.outcome }}" = "success" \
            && test "${{ steps.wasm.outcome }}" = "success" \
            && test "${{ steps.fmt.outcome }}" = "success" \
            && test "${{ steps.lint.outcome }}" = "success"

  # ---- 段2: 重い検査。CI 結果が通ったときだけ、並行して走る ----

  cosim:
    # PRではCPUの意味論に触れたときだけ (L0)。mainへのpushでは常に回す
    name: CPU照合 結果
    if: github.event_name != 'pull_request' || needs.ci.outputs.cpu == 'true'
    needs: ci
    runs-on: ubuntu-latest
    # Unicorn のビルドが刺さったときの番人。通常はキャッシュが効いて数分で終わる
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: Swatinem/rust-cache@v2

      - name: Unicornとの突き合わせ
        id: cosim
        continue-on-error: true
        run: |
          set -o pipefail
          {
            echo '## CPU照合 — `cargo test -p rustx86-cosim` (Unicornと毎命令比較)'
            echo '```'
            cargo test -p rustx86-cosim > raw.log 2>&1 || RC=$?
            grep -vE '^(   Compiling|    Finished|     Running)' raw.log | tee /dev/stderr || true
            echo '```'
            exit ${RC:-0}
          } > cosim-report.md

      - name: レポート発行
        if: always()
        run: cat cosim-report.md >> "$GITHUB_STEP_SUMMARY"

      - name: 合否判定
        run: test "${{ steps.cosim.outcome }}" = "success"
